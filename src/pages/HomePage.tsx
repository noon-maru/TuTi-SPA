import styled from "styled-components";

const HomePage = () => {
  return (
    <Container>
      <h1>TuTi 웹 페이지는 아직 개발 중입니다!</h1>
    </Container>
  );
};

const Container = styled.div`
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
`;

export default HomePage;
